version: 1
frontend:
  phases:
    preBuild:
      commands:
        - hugo version
    build:
      commands:
        - hugo --minify --buildDrafts=false
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - resources/**/*